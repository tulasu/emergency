; HTTP-сервер Asterisk — только под ARI для Go-оркестратора (originate).
; Bridge compose: traineebox идёт на 0.0.0.0:8088 из другого контейнера (host-mode и так работает).
[general]
enabled = yes
bindaddr = 0.0.0.0
bindport = 8088
